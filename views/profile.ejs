<%- include('partials/header') -%>

  <h1 class="text-4xl mb-4">Welcome <%= user.nickname %></h1>
  <%= user.email %>
  <% if (user.picture) { %>
    <img class="block py-3" src="<%= user.picture %>" width="300">
  <% } %>
  
  <p class="py-3">
    This is the content of <code class="bg-gray-700">req.user</code>.<br>
    <strong>Note:</strong> <code class="bg-gray-700">_raw</code> and <code class="bg-gray-700">_json</code> properties have been omitted.
  </p>

  <pre class="block bg-gray-700 p-4 text-sm overflow-scroll"><%= userProfile %></pre>

<%- include('partials/footer') -%>
